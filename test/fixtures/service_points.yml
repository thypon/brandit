
<% addresses = {[1, "Abbey Road, 15"] => :repair,
				[2, "Kennedy place, 2"] => :design,
				[3, "San Cristoforo Road, 33"] => :personalization,
				[4, "Warwick Avenue, 16"] => :assistance,
				[5, "Grimmauld Place, 12"] => :design,
				[6, "Golgi Road, 42"] => :repair,
				[7, "Leonardo da Vinci Place, 1"] => :assistance}%>

<% addresses.each do |address| %>
milan_<%= address.first.first %>:
 address: <%= address.first.last %>
 city: milan
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
paris_<%= address.first.first %>:
 address: <%= address.first.last %>
 city: paris
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
london_<%= address.first.first %>:
 address: <%= address.first.last%>
 city: london
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
peschici_<%= address.first.first %>:
 address: <%= address.first.last%>
 city: peschici
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
madrid_<%= address.first.first %>:
 address: <%= address.first.last%>
 city: madrid
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
madrid_<%= address.first.first %>:
 address: <%= address.first.last%>
 city: madrid
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
geneva_<%= address.first.first %>:
 address: <%= address.first.last%>
 city: geneva
 service: <%= address.last %>
<% end %>

<% addresses.each do |address| %>
rome_<%= address.first.first %>:
 address: <%= address.first.last %>
 city: rome
 service: <%= address.last %>
<% end %>