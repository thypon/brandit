<%= form_for([@product, @product_gallery_photo]) do |f| %>
  <% if @product_gallery_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_gallery_photo.errors.count, "error") %> prohibited this product_gallery_photo from being saved:</h2>

      <ul>
      <% @product_gallery_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.file_field :this %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
